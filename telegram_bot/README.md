# Telegram Bot Template

Template for generating a Telegram bot service.

## Usage

To generate the template:

```sh
go-scaffold generate ./ ../../my-bot --values ./values.yaml 
cd ../../my-bot
make init
```
